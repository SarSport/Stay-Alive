{% block field_label %}
{% spaceless %}
    <label for="{{ id }}">{{ label|trans([], 'SarSportApplicationBundle') }}</label>
{% endspaceless %}
{% endblock field_label %}

{% block form_label %}
{% spaceless %}
    <label for="{{ id }}">{{ label|trans([], 'SarSportApplicationBundle') }}</label>
{% endspaceless %}
{% endblock form_label %}

{% block widget_choice_options %}
{% spaceless %}
    {% for choice, label in options %}
        {% if _form_is_choice_group(label) %}
            <optgroup label="{{ choice|trans([], 'SarSportApplicationBundle') }}">
                {% for nestedChoice, nestedLabel in label %}
                    <option value="{{ nestedChoice }}"{% if _form_is_choice_selected(form, nestedChoice) %} selected="selected"{% endif %}>{{ nestedLabel|trans([], 'SarSportApplicationBundle') }}</option>
                {% endfor %}
            </optgroup>
        {% else %}
            <option value="{{ choice }}"{% if _form_is_choice_selected(form, choice) %} selected="selected"{% endif %}>{{ label|trans([], 'SarSportApplicationBundle') }}</option>
        {% endif %}
    {% endfor %}
{% endspaceless %}
{% endblock widget_choice_options %}
